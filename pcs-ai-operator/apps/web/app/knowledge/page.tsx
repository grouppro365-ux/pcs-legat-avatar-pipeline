'use client';import{useEffect,useState}from'react';import{API}from'../../lib/api';export default function Knowledge(){const[rows,setRows]=useState<any[]>([]);useEffect(()=>{fetch(`${API}/knowledge`,{credentials:'include'}).then(r=>r.ok?r.json():[]).then(setRows)},[]);return <><div className="top"><div><h1 style={{margin:0}}>База знаний</h1><div className="muted">AI использует только ACTIVE</div></div></div><div className="card" style={{padding:0}}>{rows.map(x=><div className="item" key={x.id}><strong>{x.title}</strong><div className="muted">{x.category} · {x.status} · auto {x.autoAnswerAllowed?'да':'нет'}</div><div>{x.description}</div></div>)}</div></>}
