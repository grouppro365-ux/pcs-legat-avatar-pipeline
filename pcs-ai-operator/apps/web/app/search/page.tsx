'use client';import{useState}from'react';import{API}from'../../lib/api';export default function Search(){const[q,setQ]=useState('');const[data,setData]=useState<any>(null);const[error,setError]=useState('');async function run(){if(!q.trim())return;const r=await fetch(`${API}/search?q=${encodeURIComponent(q)}`,{credentials:'include'});if(!r.ok){setError(await r.text());return}setData(await r.json())}return <><h1>Поиск</h1><div className="toolbar"><input className="input" style={{maxWidth:600}} value={q} onChange={e=>setQ(e.target.value)} onKeyDown={e=>{if(e.key==='Enter')void run()}} placeholder="Имя, username, телефон, сообщение, заметка"/><button className="btn" onClick={run}>Найти</button></div>{error&&<p>{error}</p>}{data&&<><h3>Клиенты</h3>{data.contacts.map((x:any)=><div className="card" key={x.id} style={{marginBottom:8}}><strong>{x.name||x.username}</strong><div className="muted">{x.status} · {x.intent||'—'}</div></div>)}<h3>Сообщения</h3>{data.messages.map((x:any)=><div className="card" key={x.id} style={{marginBottom:8}}><strong>{x.conversation.contact.name||x.conversation.contact.username}</strong><div>{x.text}</div></div>)}<h3>База знаний</h3>{data.knowledge.map((x:any)=><div className="card" key={x.id} style={{marginBottom:8}}><strong>{x.title}</strong><div>{x.description}</div></div>)}</>}</>}
