'use client';import{useEffect,useState}from'react';import{API}from'../../lib/api';export default function Tasks(){const[rows,setRows]=useState<any[]>([]);async function load(){const r=await fetch(`${API}/tasks`,{credentials:'include'});if(r.ok)setRows(await r.json())}async function done(id:string){await fetch(`${API}/tasks/${id}`,{method:'PATCH',credentials:'include',headers:{'content-type':'application/json'},body:JSON.stringify({completedAt:new Date().toISOString()})});await load()}useEffect(()=>{void load()},[]);return <><h1>Задачи</h1><div className="card" style={{padding:0}}>{rows.map(x=><div className="item" key={x.id}><strong>{x.title}</strong><div className="muted">{x.contact?.name||x.contact?.username} · {x.priority} · {x.dueAt?new Date(x.dueAt).toLocaleString():'без срока'}</div><button className="btn secondary" style={{marginTop:8}} onClick={()=>done(x.id)}>Выполнено</button></div>)}{!rows.length&&<div className="item muted">Нет открытых задач</div>}</div></>}
