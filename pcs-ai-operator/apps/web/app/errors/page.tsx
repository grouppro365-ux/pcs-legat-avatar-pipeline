'use client';import{useEffect,useState}from'react';import{API}from'../../lib/api';export default function Errors(){const[rows,setRows]=useState<any[]>([]);async function load(){const r=await fetch(`${API}/errors`,{credentials:'include'});if(r.ok)setRows(await r.json())}async function retry(id:string){await fetch(`${API}/errors/${id}/retry`,{method:'POST',credentials:'include'});await load()}useEffect(()=>{void load()},[]);return <><h1>Ошибки</h1><div className="card" style={{padding:0}}>{rows.map(x=><div className="item" key={x.id}><strong>{x.operation} · {x.queue}</strong><div>{x.error}</div><div className="muted">Попыток: {x.attempts} · {new Date(x.createdAt).toLocaleString()} {x.retriedAt?'· повтор запущен':''}</div><button className="btn secondary" style={{marginTop:8}} onClick={()=>retry(x.id)}>Повторить</button></div>)}{!rows.length&&<div className="item muted">Терминальных ошибок нет</div>}</div></>}
