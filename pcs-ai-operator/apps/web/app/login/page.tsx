'use client';import{useState}from'react';import{API}from'../../lib/api';export default function Login(){const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[msg,setMsg]=useState('');async function submit(e:any){e.preventDefault();const r=await fetch(`${API}/auth/login`,{method:'POST',credentials:'include',headers:{'content-type':'application/json'},body:JSON.stringify({email,password})});if(r.ok)location.href='/';else setMsg('Неверный логин или пароль')}return <div className="card" style={{maxWidth:420,margin:'70px auto'}}><h1>Вход</h1><form onSubmit={submit}><p><input className="input" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)}/></p><p><input className="input" type="password" placeholder="Пароль" value={password} onChange={e=>setPassword(e.target.value)}/></p><button className="btn">Войти</button></form>{msg&&<p>{msg}</p>}</div>}
